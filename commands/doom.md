---
description: Quick pessimistic take — the worst cases and failure modes for a smaller idea, snippet, or decision. The everyday workhorse.
argument-hint: [idea / snippet / decision]
---

Read the `pessimist` skill, then give the fast pessimistic read.

Target: $ARGUMENTS
(If empty, ask what to doom in one line, or use the current context.)

1. **Comprehend it** quickly (`references/comprehension.md`) — enough to be specific, not generic. If you genuinely can't tell what it is, ask one sharp question first.
2. **Single-pass sweep** of the failure taxonomy (`references/failure-taxonomy.md`) — touch every dimension, but stay compact. No fan-out unless the target turns out to be bigger than it looked.
3. **Chain the credible failures to their worst realistic end** (`references/worst-case-projection.md`).
4. **Deliver a compact doom report** (`references/doom-report-format.md`): worst-first, grounded in the actual target, with a tight coverage sweep so nothing is silently skipped.

Pure doom — failures and worst cases only, no fixes.
